apiVersion: v1
kind: ConfigMap
metadata:
  name: amr-engine-config
  namespace: amr-engine
  labels:
    app: amr-engine
data:
  # Environment configuration
  LOG_LEVEL: "INFO"
  AMR_RULES_PATH: "amr_engine/rules/eucast_v_2025_1.yaml"
  # Redis configuration (disabled for local deployment)
  REDIS_ENABLED: "false"
  # Security configuration
  PSEUDONYMIZATION_ENABLED: "true"
  PSEUDONYMIZATION_STORAGE_PATH: "/tmp/pseudonym_storage"
  # API configuration
  API_TITLE: "AMR Classification Engine"
  API_VERSION: "1.0.0"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: amr-engine-rules
  namespace: amr-engine
  labels:
    app: amr-engine
data:
  # This will be populated from the actual rules file
  eucast_v_2025_1.yaml: |
    # Placeholder - will be populated from actual file